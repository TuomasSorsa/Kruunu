package com.example.kruunu;

import androidx.appcompat.app.AppCompatActivity;

import android.os.Bundle;

/**'
 * OptionsMenu used to display all options that can be changed.
 * This is not the finished version. Check the asetusmenuV3 branch from GitHub
 * for the complete version. (It didn't make it in time for the project demo...)
 */
public class OptionsMenu extends AppCompatActivity {

    @Override
    protected void onCreate(Bundle savedInstanceState) {
        super.onCreate(savedInstanceState);
        setContentView(R.layout.activity_options_menu);
    }
}
